[project]
name = "systematics-black-midi"
version = "1.0.0"
description = "Systematics Black MIDI community site on Cloudflare Workers"
requires-python = ">=3.13"
dependencies = [
    "flask",
    "werkzeug"
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
